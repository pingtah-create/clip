# Search queries the daemon runs every cycle. One query per line. Lines starting
# with # are ignored. The daemon picks the top fresh result per query (default 1)
# and runs it through the pipeline.
#
# Good queries are specific. Better:
#   warren buffett 2026 interview
#   elon musk interview
#   cathie wood interview
# Worse:
#   investing
#   stocks
#
# The daemon uses recency + the query rank — so put queries here that you'd be
# happy clipping the *latest* result for. If the top hit is a year old, it'll
# be skipped (DAEMON_MAX_VIDEO_AGE_DAYS, default 14).

# --- Active queries ---
# REAL view data (June 23, 63 mature clips): top clips are Pabrai 725, Wood 686,
# Dalio 120, Gieger 103. KEY LEARNING: the winner is NOT a single speaker — it's the
# CLIP PATTERN (shocking stat / hidden danger / curiosity gap), which travels across
# speakers (Pabrai & Gieger broke out unexpectedly). So keep a BROAD speaker pool and
# let the picker's winner-pattern do the filtering, rather than betting on one name.

# Proven breakout speakers
cathie wood interview podcast
mohnish pabrai interview podcast
ray dalio interview podcast
bill ackman recent interview podcast

# Broad net — value/macro/tech voices that can surface a pattern-fit clip
chamath palihapitiya interview
ai revolution investing podcast 2026
value investing masterclass podcast
recession risk 2026 podcast
# balaji srinivasan interview
